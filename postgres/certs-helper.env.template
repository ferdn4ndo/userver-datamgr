# HTTP-01 target for nginx-proxy + acme-companion (same pattern as userver-web).
# Deploy (userver) sets VIRTUAL_HOST / LETSENCRYPT_* via prepare_virtual_host.
# Leave LETSENCRYPT_EMAIL empty when userver-web/letsencrypt/.env sets DEFAULT_EMAIL once.
VIRTUAL_HOST=
LETSENCRYPT_HOST=
LETSENCRYPT_EMAIL=
