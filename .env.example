# Optional Docker Compose variable substitution (see docker-compose.yml).
# The userver orchestrator creates userver-datamgr/.env with prod values when USERVER_MODE=prod.
POSTGRES_SSL_CERT_BASENAME=
USERVER_WEB_CERTS_DIR=./postgres/.nginx-certs-stub
